{
  "version": 3,
  "configurePresets": [
    {
      "name": "default",
      "displayName": "Default",
      "description": "Use the platform default CMake generator",
      "binaryDir": "${sourceDir}/build"
    },
    {
      "name": "windows-msvc",
      "displayName": "Windows MSVC x64",
      "description": "Build with Visual Studio 2022 or Build Tools. Both WinMM and Windows MIDI Services (WMS) are compiled in; the backend is selected at runtime. Requires the Windows MIDI Services SDK (see README).",
      "generator": "Visual Studio 17 2022",
      "architecture": {
        "value": "x64",
        "strategy": "external"
      },
      "binaryDir": "${sourceDir}/build"
    }
  ],
  "buildPresets": [
    {
      "name": "default",
      "configurePreset": "default"
    },
    {
      "name": "release",
      "configurePreset": "default",
      "configuration": "Release"
    },
    {
      "name": "windows-release",
      "configurePreset": "windows-msvc",
      "configuration": "Release"
    }
  ]
}
