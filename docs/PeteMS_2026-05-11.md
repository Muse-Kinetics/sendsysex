Update from Pete at Microsoft:

Couldn't clone the repo due to mixed ssh and https in use with the submodules. Manually cloned the submodules. But then got a bunch of ambiguous symbol errors in SendSysEx.vckproj with `RtMidiIn` and `RtMidiOut` 

I commented out your forward definitions for `RtMidiIn` and `RtMidiOut` in `kmiDevice.h` and then got it to compile.


It's not clear to me how you generate a WMS backed version of this project. WinMM and WinRT/UWP are in the cmakelists.txt and the presets json. I didn't see anyhting for the new SDK or in the fork of rtMIDI.

The default seems to generate WinMM only

On the plus side, it recognizes the SoftStep.

G:\Github\kmi\sendsysex\build\Debug>SendSysEx --list-normalized
Chunk size: 48 bytes, delay: 2 ms
Sending identity request to: SoftStep 25
Port translations for SoftStep:
Input (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 26): MIDIOUT2 (SoftStep) 26 -> softstep expander [expander]
Output (RtMidi port 27): MIDIOUT3 (SoftStep) 27 -> softstep cv out [cv_out]


I don't see anything in this project which imports the headers for WMS, or executes cppwinrt, so I'm guessing that is not included in this source.

Pete Brown — Yesterday at 5:27 PM
So I tried it with WinMM and your maximum chunk size. Other than the port not being ready immediately, iot appears the whole lot transferred in 4 seconds?

The SoftStep I have here appears to be working fine after all this

G:\Github\kmi\sendsysex\build\Debug>SendSysEx --fw-update SoftStep -cs 250000
Chunk size: 250000 bytes, delay: 2 ms
Automatic firmware update mode for SoftStep version 2.0.5
Loading "syx/SoftStep/Softstep-enter-bootloader.syx"
Loading "syx/SoftStep/Softstep_Firmware_v2.0.5.syx"
Waiting 1 second(s) for SoftStep to connect...
Sending identity request to: SoftStep 25
Port translations for SoftStep:
Input (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 26): MIDIOUT2 (SoftStep) 26 -> softstep expander [expander]
Output (RtMidi port 27): MIDIOUT3 (SoftStep) 27 -> softstep cv out [cv_out]
Detected application port: SoftStep 25
Opening port: SoftStep 25
Sending bootloader-entry to port: SoftStep 25
[##############################] pkt   21 B  sent     21/    21 B  left      0 B  elapsed 00:00  eta 00:00
Successfully sent "syx/SoftStep/Softstep-enter-bootloader.syx" to MIDI OUT port: SoftStep 25, size: 21 bytes.
Waiting 1 second(s) for bootloader mode...
Sending identity request to: SoftStep Bootloader 26
Found bootloader port: SoftStep Bootloader 26
Opening port: SoftStep Bootloader 26
Sending firmware to port: SoftStep Bootloader 26

MidiOutWinMM::sendMessage: error sending sysex message.

Port not ready, retrying in 3 second(s)...
Opening port: SoftStep Bootloader 26
Sending firmware to port: SoftStep Bootloader 26
[##############################] pkt 102787 B  sent 102787/102787 B  left      0 B  elapsed 00:04  eta 00:00
Successfully sent "syx/SoftStep/Softstep_Firmware_v2.0.5.syx" to MIDI OUT port: SoftStep Bootloader 26, size: 102787 bytes.
Waiting 1 second(s) for the device to return to application mode...
Sending identity request to: SoftStep 26
Port translations for SoftStep:
Input (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 26): SoftStep 26 -> softstep control surface [control_surface]
Output (RtMidi port 27): MIDIOUT2 (SoftStep) 27 -> softstep expander [expander]
Output (RtMidi port 28): MIDIOUT3 (SoftStep) 28 -> softstep cv out [cv_out]
Found application port: SoftStep 26
Automatic update flow finished.
So then I told it to use a delay of 0ms

G:\Github\kmi\sendsysex\build\Debug>SendSysEx --fw-update SoftStep -cs 250000 -cd 0
Chunk size: 250000 bytes, delay: 0 ms
Automatic firmware update mode for SoftStep version 2.0.5
Loading "syx/SoftStep/Softstep-enter-bootloader.syx"
Loading "syx/SoftStep/Softstep_Firmware_v2.0.5.syx"
Waiting 1 second(s) for SoftStep to connect...
Sending identity request to: SoftStep 25
Port translations for SoftStep:
Input (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 26): MIDIOUT2 (SoftStep) 26 -> softstep expander [expander]
Output (RtMidi port 27): MIDIOUT3 (SoftStep) 27 -> softstep cv out [cv_out]
Detected application port: SoftStep 25
Opening port: SoftStep 25
Sending bootloader-entry to port: SoftStep 25
[##############################] pkt   21 B  sent     21/    21 B  left      0 B  elapsed 00:00  eta 00:00
Successfully sent "syx/SoftStep/Softstep-enter-bootloader.syx" to MIDI OUT port: SoftStep 25, size: 21 bytes.
Waiting 1 second(s) for bootloader mode...
Sending identity request to: SoftStep Bootloader 26
Found bootloader port: SoftStep Bootloader 26
Opening port: SoftStep Bootloader 26
Sending firmware to port: SoftStep Bootloader 26

MidiOutWinMM::sendMessage: error sending sysex message.

Port not ready, retrying in 3 second(s)...
Opening port: SoftStep Bootloader 26
Sending firmware to port: SoftStep Bootloader 26
[##############################] pkt 102787 B  sent 102787/102787 B  left      0 B  elapsed 00:04  eta 00:00
Successfully sent "syx/SoftStep/Softstep_Firmware_v2.0.5.syx" to MIDI OUT port: SoftStep Bootloader 26, size: 102787 bytes.
Waiting 1 second(s) for the device to return to application mode...
Sending identity request to: SoftStep 26
Port translations for SoftStep:
Input (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 26): SoftStep 26 -> softstep control surface [control_surface]
Output (RtMidi port 27): MIDIOUT2 (SoftStep) 27 -> softstep expander [expander]
Output (RtMidi port 28): MIDIOUT3 (SoftStep) 28 -> softstep cv out [cv_out]
Found application port: SoftStep 26
Automatic update flow finished.
So do you see different results than that?

Pete Brown — Yesterday at 5:34 PM
with a chunk size of 1000, it still takes about 4 seconds.

G:\Github\kmi\sendsysex\build\Debug>SendSysEx --fw-update SoftStep -cs 1000 -cd 0
Chunk size: 1000 bytes, delay: 0 ms
Automatic firmware update mode for SoftStep version 2.0.5
Loading "syx/SoftStep/Softstep-enter-bootloader.syx"
Loading "syx/SoftStep/Softstep_Firmware_v2.0.5.syx"
Waiting 1 second(s) for SoftStep to connect...
Sending identity request to: SoftStep 25
Port translations for SoftStep:
Input (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 26): MIDIOUT2 (SoftStep) 26 -> softstep expander [expander]
Output (RtMidi port 27): MIDIOUT3 (SoftStep) 27 -> softstep cv out [cv_out]
Detected application port: SoftStep 25
Opening port: SoftStep 25
Sending bootloader-entry to port: SoftStep 25
[##############################] pkt   21 B  sent     21/    21 B  left      0 B  elapsed 00:00  eta 00:00
Successfully sent "syx/SoftStep/Softstep-enter-bootloader.syx" to MIDI OUT port: SoftStep 25, size: 21 bytes.
Waiting 1 second(s) for bootloader mode...
Sending identity request to: SoftStep Bootloader 26
Found bootloader port: SoftStep Bootloader 26
Opening port: SoftStep Bootloader 26
Sending firmware to port: SoftStep Bootloader 26

MidiOutWinMM::sendMessage: error sending sysex message.

Port not ready, retrying in 3 second(s)...
Opening port: SoftStep Bootloader 26
Sending firmware to port: SoftStep Bootloader 26
[##############################] pkt  787 B  sent 102787/102787 B  left      0 B  elapsed 00:04  eta 00:00
Successfully sent "syx/SoftStep/Softstep_Firmware_v2.0.5.syx" to MIDI OUT port: SoftStep Bootloader 26, size: 102787 bytes.
Waiting 1 second(s) for the device to return to application mode...
Sending identity request to: SoftStep 26
Port translations for SoftStep:
Input (RtMidi port 25): SoftStep 25 -> softstep control surface [control_surface]
Output (RtMidi port 26): SoftStep 26 -> softstep control surface [control_surface]
Output (RtMidi port 27): MIDIOUT2 (SoftStep) 27 -> softstep expander [expander]
Output (RtMidi port 28): MIDIOUT3 (SoftStep) 28 -> softstep cv out [cv_out]
Found application port: SoftStep 26
Automatic update flow finished.
Pete Brown — Yesterday at 5:47 PM
One thing I've noticed that rtMidi does is if the number of bytes is <= 3, it uses the non-sysex path for sending messages. So if you send < 4 bytes as the last chunk, the code path is going to be completely different. 

And I can see now in our wdmaud2.drv code where that will fail because we don't think we're in sysex in that code.

So the way rtMidi code works is making some bad assumptions about the data size, and needs to be accounted for.

I'm logging a bug internally on this as well, but this is just an incorrect assumption

void MidiOutWinMM :: sendMessage( const unsigned char *message, size_t size )
{
  if ( !connected_ ) return;

  unsigned int nBytes = static_cast<unsigned int>(size);
  if ( nBytes == 0 ) {
    errorString_ = "MidiOutWinMM::sendMessage: message argument is empty!";
    error( RtMidiError::WARNING, errorString_ );
    return;
  }

  MMRESULT result;
  WinMidiData *data = static_cast<WinMidiData *> (apiData_);
  if ( nBytes > 3 ) { // Sysex message

    // Allocate buffer for sysex data.
    char *buffer = (char *) malloc( nBytes );
    if ( buffer == NULL ) {
      errorString_ = "MidiOutWinMM::sendMessage: error allocating sysex message memory!";
      error( RtMidiError::MEMORY_ERROR, errorString_ );
      return;
    }

    ...
  }
  else { // Channel or system message.

      ...    
    }
  }
}
Actually, I need to cancel the bug on this internally. midiOutShortMsg is only for non-SysEx messages. It's expecting only non-sysex messages.
(also, I just realized that what I thought was your fork of rtMIDI is the main repo, so that's why no WMS code)

Pete Brown — Yesterday at 6:08 PM
https://github.com/thestk/rtmidi/issues/373